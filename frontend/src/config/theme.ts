import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
	palette: {
		primary: {
			main: "#6D74F2"
		},
		secondary: {
			main: "#D99E6A",
			contrastText: "#000"
		}
	}
});

export default theme;
