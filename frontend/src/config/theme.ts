import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
	palette: {
		primary: {
			main: "#6D74F2",
		},
		secondary: {
			main: "#f2eb6d",
			contrastText: "#000",
		},
	},
});

export default theme;
